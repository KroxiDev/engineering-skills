---
name: wait-what
description: Detente. Ese último mensaje no fue claro — vuelve a explicarlo.
disable-model-invocation: true
---

Lee por completo `${CLAUDE_PLUGIN_ROOT}/.agents/skills/wait-what/SKILL.md` y sigue sus instrucciones.

Resuelve desde `${CLAUDE_PLUGIN_ROOT}/.agents/skills/wait-what/` cualquier ruta relativa a `references/`, `scripts/` o `assets/`.
