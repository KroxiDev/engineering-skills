---
name: git-worktree
description: Prepara worktrees para implementar varias tareas del mismo proyecto en paralelo sin sobrescribir archivos entre agentes. Solo por invocación explícita del usuario.
license: MIT
disable-model-invocation: true
---

Lee por completo `${CLAUDE_PLUGIN_ROOT}/.agents/skills/git-worktree/SKILL.md` y sigue sus instrucciones.

Resuelve desde `${CLAUDE_PLUGIN_ROOT}/.agents/skills/git-worktree/` cualquier ruta relativa a `references/`, `scripts/` o `assets/`.
